# OpenClaw Gateway — Nginx Reverse Proxy
# Generated by deploy.sh from openclaw.conf.tmpl

upstream openclaw_backend {
    server 127.0.0.1:18789;
}

server {
    listen 80;
    server_name $SERVER_NAME;

    # Redirect HTTP to HTTPS (uncomment when SSL is configured)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://openclaw_backend;
        proxy_http_version 1.1;

        # WebSocket support (required for OpenClaw gateway)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Forward client info
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for long-running agent sessions
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_connect_timeout 10s;
    }
}

# HTTPS server block (uncomment and configure with your SSL certs)
# server {
#     listen 443 ssl http2;
#     server_name $SERVER_NAME;
#
#     ssl_certificate     /etc/letsencrypt/live/$SERVER_NAME/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/$SERVER_NAME/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://openclaw_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $remote_addr;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         proxy_send_timeout 300s;
#         proxy_connect_timeout 10s;
#     }
# }
